# 2025년도 2회 기출문제

관련 개념: [암호화·인증](../concepts/04-crypto-auth.md) · [시스템 보안](../concepts/03-system.md) · [법·제도](../concepts/06-law-practice.md)

## 1. PAM 모듈 관련 문제

(A) account / (B) auth / (C) session

## 2. 윈도우 파일시스템 관련 문제

**정답: NTFS**

## 3. 종단 암호화 관련 용어

**정답: 종단간 암호화(E2EE, End-to-End Encryption)**

## 4. DLP 관련 문제

**정답: DLP 솔루션**

## 5. 취약한 코드와 함수 문제

(A) 버퍼 오버플로우(또는 스택 버퍼 오버플로우) / (B) strcpy() 함수

## 6. 리눅스 암호화된 패스워드 관련 파일

**정답: /etc/shadow**

## 7. DNS 동작 방식 관련 문제

(A) UDP(또는 TCP) / (B) DNS 캐싱 / (C) TTL

## 8. 위험분석 접근 방법

(A) 기준선 접근법 / (B) 상세 위험 분석 / (C) 복합 접근법

## 9. 정보통신망 관련 용어

**정답: 정보통신망**

## 10. ShellShock 취약점을 이용한 웹쉘 생성 코드 (b.php)

**정답: 웹쉘(WebShell)**

## 11. 디렉터리 리스팅 취약점

**정답: Options 지시자에 설정된 Indexes 옵션을 삭제한다**

## 12. 리눅스 로그 확인 명령어

**정답: lastcomm**

---

## 13. 모바일 앱 인증서 고정(Certificate Pinning)

**(A) 정의 및 목적**: 인증서 고정 기술은 모바일 앱이 특정 서버와 SSL/TLS 통신할 때 신뢰할 수 있는 특정 서버의 인증서(또는 공개키)를 앱에 고정하여, 중간에 공격자에 의해 조작되는 중간자 공격(MITM)을 방지하는 기술

**(B) 인증서 고정 핵심요소 3가지**

1. 신뢰할 인증서(또는 공개키): 앱에 하드코딩되거나 앱 내부 리소스에 포함되어 특정 서버의 인증서(또는 공개키)를 명시
2. 검증 로직 구현: SSL/TLS 협상(핸드셰이크) 과정에서 서버로부터 받은 인증서가 고정된 것과 일치하는지 검증하는 로직 구현
3. 오류 처리 및 실패 대응: 고정된 인증서와 불일치 시 연결을 차단하거나 오류를 남기고 사용자에게 알리는 로직 구현

**(C) 인증서 고정 우회 방법 2가지**

1. Frida/Objection 등으로 런타임 패치: SSLContext, TrustManager, HostNameVerifier 등을 후킹하여 인증서 검증 로직을 우회
2. 앱 리패키징 및 재서명: 앱의 인증서 핀 고정 로직을 수정 후 다시 패키징하여 검증을 우회. 다만 루팅된 환경이나 디버깅 허용된 앱에서만 효과적

## 14. 파일 업로드 취약점 관련 문제

**(A) 취약점**: 파일 업로드 취약점

**(B) 우회 기법**: 취약한 PHP 게시판 코드는 업로드 파일의 파일타입(MIME 타입)에 대해서만 유효성 검증을 수행. 공격자는 웹쉘을 업로드하면서 요청 메시지의 파일타입(Content-Type 헤더 필드)을 허용하는 이미지 파일 타입(예: image/gif 등)으로 변조하여 유효성 검증을 우회할 수 있음

**(C) 공격 성공 조건**: 업로드된 웹쉘을 실행하기 위해서는 파일 업로드 디렉터리에 있는 웹쉘을 공격자가 외부에서 URL을 통해 호출할 수 있어야 하며, 호출된 웹쉘(예: PHP 파일)이 서버 사이드 스크립트로 동작할 수 있도록 실행 권한이 있어야 한다

## 15. 네트워크 보안 관제 3가지 구성요소와 역할

1. **에이전트(Agent)**: 각종 보안 장비 및 서버, 네트워크에 설치되어 해당 시스템에 적합한 설정에 따라 로그 정보를 실시간으로 중앙관제센터에 전송
2. **정보수집 서버(Information Collection Server)**: 각각의 에이전트에서 보낸 다량의 정보를 수집 및 처리하여 데이터베이스에 저장
3. **통합관제용 서버(Security Monitoring System)**: 각종 이벤트의 로그 분석을 수행하고, 다양한 정보를 종합적으로 분석하며 상황에 따라 관제 담당자들을 지원

## 16. ISMS-P 식별된 위험 처리 방법 4가지

1. **위험 수용(Risk Acceptance)**: 현재의 위험을 받아들이고 잠재적 손실 비용을 감수하는 전략. 위험을 완전히 제거할 수는 없으므로 일정 수준 이하의 위험을 감수하고 사업을 진행
2. **위험 감소(Risk Reduction)**: 위험을 감소시킬 수 있는 대책을 채택하여 구현하는 전략. 대책에 의한 비용과 감소하는 위험의 크기를 비교하는 비용 대비 효과 분석을 통해 효과적인 대책을 선정
3. **위험 전가(Risk Transfer)**: 위험의 잠재적 손실 비용을 보험이나 외부 등 제3자에게 이전시키는 전략
4. **위험 회피(Risk Avoidance)**: 위험이 존재하는 프로세스나 사업을 수행하지 않고 포기하는 전략

---

## 17. 윈도우 이벤트 로그 관련 문제

가. **최대 이벤트 로그 크기 계산식**: 단일 이벤트 최대 크기(500 바이트) × 하루 이벤트 발생량(1,000개) × 보관 기간(30일)

나. **계산값**: 15,000,000 바이트

다. **설정 경로**: 윈도우 이벤트 뷰어 → 해당 로그 선택 → 속성 → 일반 → 최대 로그 크기 항목 설정 (또는 시작 > 실행 > `eventvwr.msc` > 해당 로그 선택 > 속성 > 일반 > 최대 로그 크기 항목 설정)

## 18. CCTV 설치 및 운영 관련 문제

**(A) 회사 사옥 외부 조치 사항**: 회사 사옥 외부와 같이 공개된 장소에서 CCTV를 설치·운영할 경우에는 시설 안전 및 화재 예방 목적 등 개인정보보호법 제25조에서 정하는 사유에 해당하는 경우에만 가능하고, 정보주체가 쉽게 인식할 수 있도록 다음 사항이 포함된 안내판을 설치하는 등 필요한 조치를 해야 함: 설치 목적 및 장소 / 촬영 범위 및 시간 / 관리책임자의 연락처 / 그 밖에 대통령령으로 정하는 사항

**(B) 회사 사옥 내부(출입 통제 구역) 조치 사항**: 출입 통제 구역인 회사 사옥 내부는 비공개된 장소로 개인정보보호법 제25조가 적용되지 않고, CCTV를 통해 수집한 개인영상정보는 개인정보에 해당하므로 정보주체의 동의 등 개인정보보호법 제15조1항에 따른 경우 또는 법률에서 특별한 규정이 있는 경우에만 설치·운영이 가능함. 따라서 비공개된 장소에 CCTV를 설치·운영하는 경우에는 촬영 범위에 포함된 모든 정보주체의 동의를 받는 것이 바람직하고, 안내판 설치나 보호조치 등은 공개된 장소에 설치된 CCTV 규정을 준용하는 것을 권장
